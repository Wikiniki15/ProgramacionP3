<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ProgramacionP3.VistaModelo"
             x:Class="ProgramacionP3.Vista.PaisesConsultados"
             Title="JOPaisesConsultados">
    
    <ContentPage.BindingContext>
        <vm:BuscarPais />
    </ContentPage.BindingContext>
    
    <StackLayout>
        <Label x:Name="jolalla_lblPaisesConsultados"
            Text="Países Consultados" />

        <ListView x:Name="jolalla_lstPaises" 
                  ItemsSource="{Binding PaisesConsultados}">
            
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Display}" />
                </DataTemplate>
            </ListView.ItemTemplate>
            
        </ListView>
    </StackLayout>
</ContentPage>
